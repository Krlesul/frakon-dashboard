import type{DashboardEmergencyExecutionJournalEntry}from'./dashboard-emergency-execution-journal';
export type DashboardEmergencyExecutionJournalRecoveryKind='retry'|'verify-first'|'completed'|'blocked';
export interface DashboardEmergencyExecutionJournalRecovery{kind:DashboardEmergencyExecutionJournalRecoveryKind;reason:string}
export function decideDashboardEmergencyExecutionJournalRecovery(entry:DashboardEmergencyExecutionJournalEntry|undefined):DashboardEmergencyExecutionJournalRecovery{if(!entry)return{kind:'blocked',reason:'No execution journal evidence exists.'};if(entry.phase==='verified')return{kind:'completed',reason:'The physical state was verified after dispatch.'};if(entry.phase==='failed')return{kind:'retry',reason:'The recorded attempt failed and may be retried with fresh confirmation.'};if(entry.phase==='prepared')return{kind:'retry',reason:'The action was durably prepared but never entered dispatch.'};if(entry.phase==='dispatching')return{kind:'verify-first',reason:'Restart occurred inside the dispatch uncertainty window; the service call may have reached Home Assistant.'};return{kind:'verify-first',reason:'The service call returned and physical device state must be verified before any retry.'}}
