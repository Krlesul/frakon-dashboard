import{describe,expect,it}from'vitest';import{maintainDashboardEmergencyResolvedRecoveries}from'./dashboard-emergency-recovery-maintenance';import type{DashboardEmergencyHistoryState}from'./dashboard-emergency-history';function storage(initial:string){const data=new Map<string,string>([['frakon.dashboard.emergency-recovery-resolution.v2',initial]]);return{getItem:(key:string)=>data.get(key)??null,setItem:(key:string,value:string)=>{data.set(key,value)}}}describe('Emergency recovery maintenance',()=>{it('keeps protected history occurrences while pruning expired unrelated records',()=>{const now=400*24*60*60*1000,history:DashboardEmergencyHistoryState={active:[{signature:'water:1',itemId:'a',sourceEntityIds:[],reasons:[],startedAt:100}],recent:[]},s=storage(JSON.stringify([{occurrenceId:'water:1@@100',incidentSignature:'water:1',resolvedAt:1},{occurrenceId:'gas:1@@10',incidentSignature:'gas:1',resolvedAt:1}]));expect(maintainDashboardEmergencyResolvedRecoveries(s,history,now).map(record=>record.occurrenceId)).toEqual(['water:1@@100'])})});
