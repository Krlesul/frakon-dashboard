import{describe,expect,it,vi}from'vitest';import{verificationTargetForService,verifyDashboardEmergencyAction}from'./dashboard-emergency-action-verification';describe('Emergency action verification',()=>{it('maps water shutoff services to safe terminal states',()=>{expect(verificationTargetForService('valve','close_valve','valve.main_water')).toEqual({entityId:'valve.main_water',expectedStates:['closed']});expect(verificationTargetForService('switch','turn_off','switch.main_water')).toEqual({entityId:'switch.main_water',expectedStates:['off']});});it('verifies when HA reports the requested terminal state',async()=>{const states=['open','closing','closed'];const getState=vi.fn(()=>states.shift());expect(await verifyDashboardEmergencyAction({entityId:'valve.main_water',expectedStates:['closed']},{getState},{attempts:3,delayMs:0})).toEqual({status:'verified',actualState:'closed'});});it('fails when the device never reaches the requested state',async()=>{const getState=vi.fn(()=> 'open');const result=await verifyDashboardEmergencyAction({entityId:'valve.main_water',expectedStates:['closed']},{getState},{attempts:2,delayMs:0});expect(result.status).toBe('failed');expect(result.actualState).toBe('open');});});
