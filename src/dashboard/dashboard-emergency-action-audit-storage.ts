import type{DashboardEmergencyActionAuditEntry}from'./dashboard-emergency-action-audit';
export const DASHBOARD_EMERGENCY_ACTION_AUDIT_STORAGE_KEY='frakon.dashboard.emergency-action-audit.v1';
export function loadDashboardEmergencyActionAudit(storage:Pick<Storage,'getItem'>|undefined,key=DASHBOARD_EMERGENCY_ACTION_AUDIT_STORAGE_KEY):DashboardEmergencyActionAuditEntry[]{if(!storage)return[];try{const raw=storage.getItem(key);if(!raw)return[];const parsed=JSON.parse(raw);return Array.isArray(parsed)?parsed.filter(valid):[]}catch{return[]}}
export function saveDashboardEmergencyActionAudit(storage:Pick<Storage,'setItem'>|undefined,entries:DashboardEmergencyActionAuditEntry[],key=DASHBOARD_EMERGENCY_ACTION_AUDIT_STORAGE_KEY):DashboardEmergencyActionAuditEntry[]{if(storage)try{storage.setItem(key,JSON.stringify(entries))}catch{return entries}return entries}
function valid(value:unknown):value is DashboardEmergencyActionAuditEntry{if(!value||typeof value!=='object')return false;const entry=value as Record<string,unknown>;return typeof entry.id==='string'&&typeof entry.incidentSignature==='string'&&typeof entry.createdAt==='number'&&typeof entry.planId==='string'&&typeof entry.kind==='string'&&typeof entry.policyLevel==='string'}
