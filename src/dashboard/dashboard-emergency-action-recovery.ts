import type { DashboardEmergencyVerificationResult } from './dashboard-emergency-action-verification';
export type DashboardEmergencyRecoveryKind='retry'|'try-alternative'|'inspect-device'|'keep-emergency-active';
export interface DashboardEmergencyRecoveryAction { kind:DashboardEmergencyRecoveryKind; label:string; entityId?:string; }
export interface DashboardEmergencyRecoveryPlan { critical:boolean; message:string; actions:DashboardEmergencyRecoveryAction[]; }
export interface DashboardEmergencyRecoveryCandidate { entityId:string; label:string; }
export function createDashboardEmergencyRecoveryPlan(result:DashboardEmergencyVerificationResult,failedEntityId:string,candidates:DashboardEmergencyRecoveryCandidate[],locale='en'):DashboardEmergencyRecoveryPlan|undefined { if(result.status!=='failed')return undefined;const cs=locale.toLowerCase().startsWith('cs');const alternatives=candidates.filter(candidate=>candidate.entityId!==failedEntityId);const actions:DashboardEmergencyRecoveryAction[]=[{kind:'retry',label:cs?'Zkusit uzavření znovu':'Retry shutoff',entityId:failedEntityId},{kind:'inspect-device',label:cs?'Zkontrolovat zařízení':'Inspect device',entityId:failedEntityId},...alternatives.slice(0,3).map(candidate=>({kind:'try-alternative' as const,label:cs?`Použít alternativu: ${candidate.label}`:`Try alternative: ${candidate.label}`,entityId:candidate.entityId})),{kind:'keep-emergency-active',label:cs?'Ponechat havárii aktivní':'Keep emergency active'}];return{critical:true,message:cs?'Uzavření vody nebylo potvrzeno. Voda může stále téct.':'Water shutoff was not confirmed. Water may still be flowing.',actions}; }
