import{describe,expect,it}from'vitest';import{dashboardEmergencyAuditStatistics}from'./dashboard-emergency-audit-statistics';import type{DashboardEmergencyActionAuditEntry}from'./dashboard-emergency-action-audit';const entry=(overrides:Partial<DashboardEmergencyActionAuditEntry>={}):DashboardEmergencyActionAuditEntry=>({id:'a',incidentSignature:'water:1',createdAt:10,planId:'p',kind:'close-water-shutoff',policyLevel:'confirm',...overrides});describe('Emergency audit statistics',()=>{it('counts multiple attempts inside one occurrence as one occurrence',()=>{const first=entry({id:'a',incidentOccurrenceId:'water:1@@100',verificationStatus:'failed'}),retry=entry({id:'b',incidentOccurrenceId:'water:1@@100',createdAt:20,verificationStatus:'verified'});expect(dashboardEmergencyAuditStatistics([retry,first])).toMatchObject({occurrences:1,attempts:2,resolved:1,failed:0})});it('counts repeated incidents with the same signature as separate occurrences',()=>{const first=entry({id:'a',incidentOccurrenceId:'water:1@@100',verificationStatus:'verified'}),second=entry({id:'b',incidentOccurrenceId:'water:1@@200',createdAt:20,verificationStatus:'failed'});expect(dashboardEmergencyAuditStatistics([second,first])).toMatchObject({occurrences:2,attempts:2,resolved:1,failed:1})});it('keeps legacy signature-only audit compatible',()=>{expect(dashboardEmergencyAuditStatistics([entry(),entry({id:'b',createdAt:20})])).toMatchObject({occurrences:1,attempts:2,pending:1})})});
