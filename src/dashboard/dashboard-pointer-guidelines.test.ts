import{describe,expect,it}from'vitest';import{dashboardPointerGuidelines}from'./dashboard-pointer-guidelines';import type{FrakonDashboardDocument}from'./layout-model';
const doc:FrakonDashboardDocument={version:1,id:'home',title:'Home',breakpoint:'desktop',columns:4,rowHeight:50,gap:10,items:[{id:'a',x:0,y:0,w:1,h:1,card:{type:'custom:a'}},{id:'b',x:2,y:0,w:1,h:1,card:{type:'custom:b'}}]};
describe('dashboard pointer guidelines',()=>{it('reports aligned horizontal anchors in pixel space',()=>{const preview=[{...doc.items[0],x:1},{...doc.items[1]}];const guides=dashboardPointerGuidelines(doc,preview,['a'],430);expect(guides.some(g=>g.axis==='y')).toBe(true)});it('returns no guides without stationary items',()=>{expect(dashboardPointerGuidelines(doc,doc.items,['a','b'],430)).toEqual([])})});
