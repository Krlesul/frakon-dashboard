import{dashboardEmergencyActionAuditOccurrenceKey,type DashboardEmergencyActionAuditEntry}from'./dashboard-emergency-action-audit';import{dashboardEmergencyIncidentStatus,type DashboardEmergencyIncidentResolution}from'./dashboard-emergency-incident-status';
export interface DashboardEmergencyAuditStatistics{occurrences:number;attempts:number;resolved:number;failed:number;pending:number;noAction:number;byResolution:Record<DashboardEmergencyIncidentResolution,number>}
export function dashboardEmergencyAuditStatistics(entries:DashboardEmergencyActionAuditEntry[]):DashboardEmergencyAuditStatistics{const groups=new Map<string,{incidentSignature:string;incidentOccurrenceId?:string}>();for(const entry of entries){const key=dashboardEmergencyActionAuditOccurrenceKey(entry);if(!groups.has(key))groups.set(key,{incidentSignature:entry.incidentSignature,incidentOccurrenceId:entry.incidentOccurrenceId})}const byResolution:Record<DashboardEmergencyIncidentResolution,number>={resolved:0,failed:0,pending:0,'no-action':0};for(const group of groups.values()){const status=dashboardEmergencyIncidentStatus(entries,group.incidentSignature,group.incidentOccurrenceId);byResolution[status.resolution]++}return{occurrences:groups.size,attempts:entries.length,resolved:byResolution.resolved,failed:byResolution.failed,pending:byResolution.pending,noAction:byResolution['no-action'],byResolution}}
