import{describe,expect,it}from'vitest';import{decideDashboardEmergencyExecutionJournalRecovery}from'./dashboard-emergency-execution-journal-recovery';import type{DashboardEmergencyExecutionJournalEntry}from'./dashboard-emergency-execution-journal';const entry=(phase:DashboardEmergencyExecutionJournalEntry['phase']):DashboardEmergencyExecutionJournalEntry=>({key:'k',incidentSignature:'water:1',incidentOccurrenceId:'water:1@@100',planId:'p',phase,createdAt:1,updatedAt:2});describe('Emergency execution journal restart recovery',()=>{it('allows retry only before dispatch or after a recorded failure',()=>{expect(decideDashboardEmergencyExecutionJournalRecovery(entry('prepared')).kind).toBe('retry');expect(decideDashboardEmergencyExecutionJournalRecovery(entry('failed')).kind).toBe('retry')});it('requires verification throughout the uncertain or dispatched window',()=>{expect(decideDashboardEmergencyExecutionJournalRecovery(entry('dispatching')).kind).toBe('verify-first');expect(decideDashboardEmergencyExecutionJournalRecovery(entry('dispatched')).kind).toBe('verify-first')});it('treats verified as completed and missing evidence as blocked',()=>{expect(decideDashboardEmergencyExecutionJournalRecovery(entry('verified')).kind).toBe('completed');expect(decideDashboardEmergencyExecutionJournalRecovery(undefined).kind).toBe('blocked')})});
